// Test setup file
// This file runs before each test suite

// Suppress console logs during tests (optional)
// global.console = {
//   ...console,
//   log: jest.fn(),
//   debug: jest.fn(),
//   info: jest.fn(),
//   warn: jest.fn(),
// };

// Note: jest.setTimeout and afterEach are available globally in Jest
// No need to import them explicitly
